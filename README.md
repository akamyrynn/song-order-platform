# Custom Song Platform

Платформа для создания персонализированных песен на заказ с веб-интерфейсом, конструктором параметров и административной панелью.

## Возможности

- 🎵 **Конструктор песен** - многошаговая форма для создания заказа
- 🎨 **Уникальный дизайн** - характерная типографика, анимации, градиенты
- 👨‍💼 **Админ-панель** - управление заказами, статусами
- 📊 **Дашборд** - статистика и аналитика заказов
- 🔍 **Фильтрация** - поиск и фильтрация заказов по статусу
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## Технологии

- **Frontend**: Next.js 14, React, TypeScript
- **Styling**: CSS Modules, CSS Variables
- **Database**: PostgreSQL (Supabase)
- **ORM**: Prisma
- **Fonts**: Space Grotesk, Inter
- **Validation**: Zod
- **Testing**: Jest, fast-check

## Установка

1. **Клонируйте репозиторий**

```bash
git clone <repository-url>
cd custom-song-platform
```

2. **Установите зависимости**

```bash
npm install
```

3. **Настройте переменные окружения**

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Обязательные переменные:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `NEXT_PUBLIC_SUPABASE_URL` - URL вашего Supabase проекта
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Anon ключ Supabase
- `SUPABASE_SERVICE_ROLE_KEY` - Service role ключ Supabase

4. **Настройте базу данных**

```bash
# Сгенерируйте Prisma Client
npx prisma generate

# Примените миграции
npx prisma migrate deploy

# Или для разработки
npx prisma migrate dev
```

5. **Создайте тестового админа**

```bash
npx ts-node scripts/create-admin.ts
```

Данные для входа:
- Email: `admin@test.com`
- Password: `admin123`

6. **Запустите сервер разработки**

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Структура проекта

```
custom-song-platform/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── admin/             # Админ-панель
│   │   ├── constructor/       # Конструктор песен
│   │   ├── order-success/     # Страница успеха
│   │   └── api/               # API endpoints
│   ├── components/            # React компоненты
│   │   ├── landing/          # Компоненты landing page
│   │   ├── constructor/      # Компоненты конструктора
│   │   └── admin/            # Компоненты админки
│   ├── lib/                   # Утилиты
│   │   ├── prisma.ts         # Prisma client
│   │   ├── supabase.ts       # Supabase client
│   │   └── validation.ts     # Zod схемы
│   └── types/                 # TypeScript типы
├── prisma/
│   └── schema.prisma         # Схема базы данных
├── scripts/                   # Скрипты
└── docs/                      # Документация
```

## Использование

### Для клиентов

1. Откройте главную страницу
2. Нажмите "Создать песню"
3. Заполните форму конструктора (4 шага)
4. Получите номер заказа
5. Перейдите в Telegram-бот для уведомлений

### Для администраторов

1. Откройте `/admin`
2. Просмотрите дашборд со статистикой
3. Перейдите к списку всех заказов
4. Используйте фильтры для поиска
5. Откройте детали заказа
6. Обновите статус заказа

## API Endpoints

### Orders

- `POST /api/orders` - Создать заказ
- `GET /api/orders` - Получить список заказов (с фильтрацией)
- `GET /api/orders/:id` - Получить заказ по ID
- `PATCH /api/orders/:id` - Обновить заказ

### Параметры фильтрации

- `status` - Фильтр по статусу (new, in_progress, ready, paid, completed)
- `orderNumber` - Поиск по номеру заказа
- `startDate` - Фильтр по дате (от)
- `endDate` - Фильтр по дате (до)
- `page` - Номер страницы (по умолчанию 1)
- `limit` - Количество на странице (по умолчанию 20)

## Статусы заказов

- **new** - Новый заказ
- **in_progress** - В работе
- **ready** - Песня готова
- **paid** - Оплачен
- **completed** - Завершен

## Разработка

### Запуск тестов

```bash
npm test
```

### Проверка типов

```bash
npx tsc --noEmit
```

### Просмотр базы данных

```bash
npx prisma studio
```

## TODO

- [ ] Интеграция Telegram-бота
- [ ] Система оплаты (Stripe/PayPal)
- [ ] Загрузка файлов (AWS S3)
- [ ] Система сообщений
- [ ] JWT аутентификация для админов
- [ ] Email уведомления

## Лицензия

MIT
